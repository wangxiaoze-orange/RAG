# ===== Web 框架（SSE 手写 StreamingResponse，不依赖 sse-starlette） =====
fastapi==0.141.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20

# ===== LangChain / LangGraph（community 集成已并入 langchain-openai，无需单独装） =====
langchain==1.3.14
langchain-openai==1.4.1
langgraph==1.2.10
# langchain-text-splitters 自 1.0 起不再是 langchain-core 的依赖（切分器实现独立成包），需显式声明
langchain-text-splitters>=1.1.2,<2.0

# ===== 数据模型 =====
pydantic>=2.11
pydantic-settings>=2.7
sqlalchemy>=2.0.41
aiomysql>=0.3.2
cryptography>=44
redis>=5.2
pymilvus==2.5.4               # 与 milvusdb/milvus:v2.5.4 服务器严格匹配（勿升：新版桩要求 protobuf 5/6.x）
# setuptools 提供 pkg_resources：pymilvus 2.5.4 用它读取自身版本号
# （setuptools 82+ 已移除 pkg_resources；RHEL 系 python3.11 的 venv 默认不带 setuptools）
setuptools>=70,<82
# etcd 客户端走 etcd v3 HTTP API（httpx，见 src/config/etcd_client.py），不依赖 etcd3——
# etcd3 0.12.0 的旧 proto 桩要求 protobuf<3.21，与 pymilvus 的 >=4.22 冲突
# protobuf 锁 4.x：pymilvus 2.5.4 桩由 protoc 4.25.1 生成，protobuf 6.x 将移除其兼容路径
protobuf>=4.25,<5
minio>=7.2.20
rank-bm25>=0.2.2

# ===== 安全 =====
PyJWT>=2.10.1
bcrypt>=4.2.1

# ===== 工具 =====
httpx>=0.28.1
tiktoken>=0.13.0
arq>=0.28.0

# ===== 文档解析（MinerU 之外的降级链） =====
pypdf>=5.0
pdfplumber>=0.11.0
python-docx>=1.1.2
ddgs>=9.0.0
jieba>=0.42.1
